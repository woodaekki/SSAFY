# < 아스키 >
# 메모리는 숫자 만을 저장할 수 있는데,
# 메모리 낭비를 막기 위해
# 문자에 대해 대응되는 숫자를 정해놓고 사용하는
# 표준안 '아스키 코드' 를 제정함

# < 확장 아스키 >
# 표준 문자이외의 도형, 특수 문자 등의 부가적인 문자 추가
# 비교적 세계적 통용이 되지않음

# < 유니코드 >
# 자국의 코드 체계를 타국가가 가지지 않을 때 생기는 문제 해결 위해
# 다국적 처리를 위한 표준 마련

print(f'{ord("대"): x}') # 숫자로 전환: x는 16진수로 변환하라는 의미
print(chr(0xb300)) # 글자로 전환

# < 빅 인디언, 리틀 인디언 >
# 상위랑 하위 바이트 중 어떤 순서를 우선하여 저장할 것인가

# < 유니코드 인코딩 방식의 종류 >
# UTF-8, UTF-16, UTF-32 등 있는데,
# 각 문자 저장 방식에 차이



