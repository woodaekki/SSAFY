# n * n: 영역마다의 파리 개수
# m * m: 파리채 영역

# 1. n * n 배열에 파리를 넣는다.
# 2. m * m 배열의 파리채를 만든다.
for n1 in range():
    for n2 in range():
        sumv = 0
        for row in range(n1, n2+m):
            for col in range(n2, n2+m):
                sumv = arr[row][col]
        if

# 3. n 배열을 순회하면서 파리채 영역의 합이 가장 큰 경우를 찾는다.
