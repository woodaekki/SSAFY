# 두 수의 합을 구하는 함수
def get_sum(a, b): # 매개 변수: 함수 정의
    return a + b

# 함수 호출
num1, num2 = 5, 3
result = get_sum(num1, num2) # 인자: 함수 호출
print(result)

# print() 함수는 반환값이 없다 !!
a = print(1)
print(a) #None